{
  "name": "testing-tailwind",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "publish": "rm -rf dist && npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.8",
    "gh-pages": "^4.0.0",
    "postcss": "^8.4.14",
    "tailwindcss": "^3.1.7",
    "vite": "^3.0.0"
  }
}
